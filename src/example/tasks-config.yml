settings:
  - logLevel: info
tasks:
  - name: Task1
    function: 'example/task1.js' # Specify the corrected relative path
    conditions:
      - type: 'contains'
        property: 'dataValue'
        additionalParam: 'Order'
